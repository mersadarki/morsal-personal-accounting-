// Initial dataset used only on first run (before any data is saved on the
// device). Empty by default — populate via Settings → «بازیابی از پشتیبان»
// or the Excel importers, or paste real historical rows here before deploy.
export const SEED_TX = [];
export const SEED_BALANCES = {};
